-- This example will probably not compile but it shows that is possible to use a custom globals file
-- without kaledis overwriting it, the flag that enables this is the using_custom_globals on the project
custom_variable = 2

